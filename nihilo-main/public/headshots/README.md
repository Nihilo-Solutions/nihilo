Place your headshot(s) here so the site can load them directly.

- Filename for Sam Oakes: `SO.jpg` (place as `/public/headshots/SO.jpg`)
- Filename for Jake Ice: `JI.jpg` (optional)

Recommended: 800x800px or 400x400px, JPEG or PNG. Keep filenames exactly as above (case-sensitive on some hosts).

After adding the file, rebuild or redeploy the site (or push to your hosting provider):

```bash
npm run build
npx serve -s dist
```

If you want me to upload the file for you, attach it here or tell me the path on your machine and I can add it to the repo.